# Morning Review — 2026-04-27 (Jeff)

Autonomous overnight session. Jeff's instruction: apply chart option A,
decorate UI with Q-TRON brand icons (Demimure design judgment), defer
genuinely binary calls to JUG (= write here for morning review),
finish by morning. No live escalation.

## What landed (committed)

| # | Commit | Topic |
|---|--------|-------|
| 1 | `0bf28687` | Chart equity-unified — option A: extend x-axis to strategy inception (2026-04-10), runtime-anchor last point to live engine equity |
| 2 | `f8e8ae96` | Earlier today — equity.json date dedup at engine + state_store loader, risk-metrics `INVALID` guard, UI "Insufficient / Corrupted data" card |
| 3 | `33770f47` | Earlier today — equity-unified reads per-strategy state files instead of corrupted equity.json |
| 4 | TBD (this commit) | Brand assets + nav wordmark |

## Brand decoration — Demimure design judgment

### Decisions made

1. **Logo placement = shared nav strip only.** The text-only `<span>Q-TRON</span>` in the nav header was the highest-leverage location — visible on every page (KR + US, dashboard / lab / debug / unified / surge), single source-of-truth for the brand. Replaced with inline SVG Q-mark + gradient "Q-TRON" wordmark. Rendered size: 93 × 26 px with cyan→blue gradient + 6 px drop-shadow glow.

2. **Favicons stay per-market flag.** Both kept (KR 태극, US Stars & Stripes). Reason: when an operator runs both markets in side-by-side tabs, the flag color codes the market faster than a Q-TRON brand mark would. The Q-TRON identity already lands inside the nav strip on every page.

3. **Brand kit assembled at `shared/web/static/brand/`:**
   - `qtron-mark.svg` — 64×64 logomark (Q ring + 3 circuit traces)
   - `qtron-wordmark.svg` — 200×64 mark + "Q-TRON" text
   - `qtron-icon-gui.svg` — 256×256 desktop tile (rounded, blue)
   - `qtron-icon-exe.svg` — 256×256 cyber HUD ring
   - `qtron-icon-telegram.svg` — 512×512 round avatar (Telegram-friendly)
   - `qtron-icon-rest.svg` — 256×256 cloud + code brackets
   - `README.md` — usage map + raster-reference layout

4. **Single gradient stop set across all marks.** sky-300 → blue-500 → blue-800 (`#7dd3fc` → `#3b82f6` → `#1e40af`). One change point if we ever ship a light-theme variant.

### What I did NOT do (Demimure-flagged for JUG → Jeff in the morning)

These need an explicit go/no-go from Jeff that I shouldn't decide silently:

- **Telegram bot avatar** — `qtron-icon-telegram.svg` exists but has not been uploaded to @BotFather. Manual one-time op step (BotFather doesn't accept SVG; needs PNG export at 512×512). Decision needed: who runs the BotFather command and when.

- **Windows EXE / GUI icons** — `qtron-icon-exe.svg` / `qtron-icon-gui.svg` not wired into the build config (PyInstaller / Inno Setup). Need to know which build pipeline owns the icon path before changing it. Worst case = breaks the next .exe build silently.

- **Tagline asset (`QUANT · TRADING · ON`)** — Jeff's reference image included this as a wordmark variant but no brand-kit slot makes obvious sense for it (footer? About page? splash?). Skipped for now. Decision needed: where does the tagline live, if anywhere.

- **Raster reference originals.** Jeff's chat-attached PNG/JPEG sketches were not committed (chat-only attachments). The SVGs above were derived from those designs. If Jeff wants a forensic copy, he should drop them into `shared/web/static/brand/raster/`.

- **Light-theme variant.** Default gradient reads on dark nav strip; on a hypothetical light-theme nav the contrast drops. The themes.js system supports light mode but I haven't validated the brand mark there. JUG decision: enforce light-mode validation as a follow-up, or ship dark-only for now.

## Chart option A — caveats Jeff should know

The chart's x-axis now spans 2026-04-10 → 2026-04-24 (strategy inception → latest LIVE date), not just the LIVE 3-day window. Strategy lines are normalized against `LAB_INITIAL_CAPITAL = 100M` so the last point of each strategy series matches the per-strategy Forward Trading card's "누적" column 1:1.

**The chart will look "flat with a jump on the last day" until more EODs accumulate.** This is not a rendering bug. The corruption window left only 1~2 distinct dates per strategy in `equity_history`; forward-fill carries the earliest survivor across all intermediate days, then the runtime-anchor pins the last day to the live engine's current equity. Once the dedup-fixed engine (commit `f8e8ae96`) records a few new EODs with proper distinct dates, the curves smooth out organically — no further code change needed.

If Jeff wants smoothing immediately:
- **Option (post-hoc):** linearly interpolate between `equity_history` points. Visually pleasing but presents pseudo-data, so I held off.
- **Option (rebuild):** reconstruct daily equity from `trades_history.csv` + closing prices. Possible but a real piece of work — JUG decision required.

## Live server restart status

| Server | PID | Restart needed for tonight's commits? |
|---|---|---|
| KR 8080 | 5064 (no `--reload`) | **Yes — chart fix + nav logo + risk-metrics INVALID guard all need restart** |
| US 8081 | (`--reload` enabled) | Probably picked up by file watcher; verify by `curl /api/charts/equity-unified` returning 11-day range |

Restart command (KR):

```bash
cd kr && ../.venv64/Scripts/python.exe -m uvicorn web.app:app --host 0.0.0.0 --port 8080
```

After KR restart, expect:
- `/api/risk/metrics` returns `status: "INVALID", reason: "INSUFFICIENT_DATA"` (was: noise sharpe -9.17). UI Risk Metrics card shows amber "Insufficient data — only 3 unique trade days available; need ≥ 20 for stable risk metrics".
- `/api/charts/equity-unified?days=90` returns 11-day x-axis with strategies starting at +15.25%/+9.61%/etc and last point = current runtime equity (matches Forward Trading cards' 누적).
- Top of every page shows the new Q-TRON wordmark in the nav strip (Q logomark + gradient blue text).

## Verified tonight

- `/api/charts/equity-unified?days=90` on preview 8085: 11 dates, top3 by last-pct = breakout_trend +26.81%, liquidity_signal +19.84%, hybrid_qscore +14.19%. Matches Forward Trading card "누적" column.
- Nav wordmark renders on KR `/`, `/lab`, `/debug` at 93×26 with "Q-TRON" text on a single line (white-space: nowrap fix landed v4 → v5 cache buster).
- Brand SVG kit at `shared/web/static/brand/` with usage README.

## Open chip — out of scope tonight, mention if relevant

- The corrupt `kr/data/lab_live/equity.json` is still tracked in git (pre-existing — not my doing). Spec said "runtime data 파일은 commit 금지" but that ship sailed before this session. Leaving as-is per scope; the loader heals it on read. Worth a separate cleanup PR.

## Where to push back if anything looks wrong

- Chart still flat → Jeff likely wants linear-interpolate or trades-history reconstruction. Tell me which.
- Brand mark looks generic → drop raster originals into `shared/web/static/brand/raster/` and I'll re-derive the SVGs to match exactly.
- Light-theme issue → flag and I'll add a `prefers-color-scheme` variant.
- Wrong place chosen for the wordmark → the qnav-logo `<a>` in nav.js is the only insertion point, easy to revert.
